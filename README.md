# Alldare Ingress Gateway (`alldare-gateway`)

Dedicated OpenResty Edge Ingress Proxy microservice for the Alldare Platform.

## Features
- Dynamic CORS security resolution
- OpenResty Lua execution engine
- Upstream routing to `alldare-web` (SPA) and `alldare-api` (BFF)
- Rate limiting and SSL termination
